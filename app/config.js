export default config = {
    // Must be an absolute path and it's recommended to use the 'csv' extension (as the output file is a CSV-file)
    outputFilePath: "/home/someone/Desktop/output.csv",

    // Must be greater than 'pageToNumber'
    pageFromNumber: 290,

    // Must be lesser than 'pageFromNumber'
    pageToNumber: 74
};